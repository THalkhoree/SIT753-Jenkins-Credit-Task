pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Starting the automated CI build from GitHub'
            }
        }

        stage('Test') {
            steps {
                echo 'Running application tests'
            }
        }
    }
}
